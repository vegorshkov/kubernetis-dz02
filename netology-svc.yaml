# Selector должен совпадать с label пода "selector: app: netology-app" для обеспечения сетевой кубер связанности
apiVersion: v1
kind: Service
metadata:
  name: netology-svc
spec:
  selector:
    app: netology-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
